% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/e-signature.R
\name{eSign}
\alias{eSign}
\title{Add an externally authenticated electronic signature}
\usage{
eSign(x, signer, meaning, credential, sign, timestamp = Sys.time())
}
\arguments{
\item{x}{An initialized, currently valid \link[PhysioCore:PhysioExperiment]{PhysioCore::PhysioExperiment}.}

\item{signer}{Non-empty signer identity.}

\item{meaning}{Non-empty signature meaning.}

\item{credential}{Named list containing \code{id}, \code{algorithm}, \code{fingerprint}, and
optional non-secret \code{public_data}.}

\item{sign}{Function called once as \code{sign(challenge_raw, credential)}. It
must return a non-empty raw vector.}

\item{timestamp}{One finite \code{POSIXct} value that does not precede the audit
head.}
}
\value{
A modified copy of \code{x} with one signature and one cross-linked audit
event.
}
\description{
The callback is responsible for authenticating and authorizing the signer.
It is called exactly once with canonical challenge bytes and a sanitized
public credential descriptor. The package never accepts passwords, private
keys, API tokens, or reusable secrets.
}
\details{
The callback's algorithm and credential service determine cryptographic
strength, identity assurance, and non-repudiation properties. This function
is a technical control and does not itself establish regulatory compliance.
}
