% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deidentify.R
\name{deidentify}
\alias{deidentify}
\title{De-identify PhysioCore records}
\usage{
deidentify(
  x,
  policy = safeHarborPolicy(),
  subject_id = NULL,
  pseudonymization = NULL,
  date_key = NULL,
  audit_actor = NULL,
  audit_reason = "record de-identified"
)
}
\arguments{
\item{x}{A \code{PhysioExperiment}, \code{MultiRatePhysioExperiment},
\code{PhysioLongitudinal}, or \code{PhysioCohort} (a multi-subject container whose
subject-level \code{colData} and every subject timeline are de-identified; unlike
MultiRate/Longitudinal it has a \code{metadata} slot, so its merged report is
stored there).}

\item{policy}{A policy from \code{\link[=safeHarborPolicy]{safeHarborPolicy()}} or
\code{\link[=pseudonymizedPolicy]{pseudonymizedPolicy()}}.}

\item{subject_id}{A scalar or field-aligned subject identifier used only for
keyed date shifting.}

\item{pseudonymization}{A protected result from \code{\link[=pseudonymize]{pseudonymize()}} used to
replace direct identifiers.}

\item{date_key}{A caller-owned raw key of at least 32 bytes.}

\item{audit_actor}{Responsible actor when an input audit trail is initialized.}

\item{audit_reason}{Non-empty reason stored in linked audit events.}
}
\value{
A modified object of the same S4 class.
}
\description{
Applies an explicit field policy while preserving the input S4 class.
Assays are never inspected for identifying signal content; retained assays
are therefore listed for manual review. Reports contain paths and counts,
never removed values, keys, or re-identification material.
}
\details{
\code{MultiRatePhysioExperiment} and \code{PhysioLongitudinal} do not provide a
metadata slot. Their aggregate report is stored as a serializable
\code{deidentification} attribute, while every child \code{PhysioExperiment} keeps its
report in \code{metadata()}. Existing audit trails are linked per child.
}
